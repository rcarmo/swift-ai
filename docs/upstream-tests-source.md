# Upstream test inventory — @earendil-works/pi-ai v0.80.2 source

Canonical source: `github.com/earendil-works/pi` at `ec6311beb5b24fc918e5031173608447582d7262` (`packages/ai`).

- Test files found: **86**
- Test cases found by `test/it(...)` scan: **883**

The npm tarball omits these tests; this source checkout is the test-for-test parity reference.

| Upstream test file | Cases | Sample cases | Swift status |
|---|---:|---|---|
| `test/abort.test.ts` | 31 | should abort mid-stream; should handle immediate abort; should abort mid-stream; should handle immediate abort; should abort mid-stream | PENDING |
| `test/anthropic-adaptive-thinking-models.test.ts` | 1 | marks built-in Anthropic Messages models that use adaptive thinking | ADAPTED (`testAnthropicAdaptiveThinkingModelMetadata`) |
| `test/anthropic-cache-write-1h-cost.test.ts` | 2 | prices the 1h portion at 2x input and the rest at the 5m rate; falls back to the 5m rate when no breakdown is reported | ADAPTED (`testAnthropicCacheWrite1hCost`) |
| `test/anthropic-eager-tool-input-compat.test.ts` | 3 | sends per-tool eager_input_streaming by default; uses the legacy fine-grained tool streaming beta when eager tool input streaming is disabled; does not send the legacy fine-grained tool streaming beta when there are no tools | ADAPTED (`testAnthropicEagerToolInputCompat`) |
| `test/anthropic-eager-tool-input-e2e.test.ts` | 1 | covers every generated anthropic-messages model | PENDING |
| `test/anthropic-empty-thinking-signature-compat.test.ts` | 2 | converts empty-signature thinking to text by default; preserves empty-signature thinking when allowEmptySignature is enabled | ADAPTED (`testAnthropicEmptyThinkingSignatureCompat`) |
| `test/anthropic-force-adaptive-thinking.test.ts` | 5 | sends legacy thinking payload for custom model ids by default; sends adaptive thinking payload when compat.forceAdaptiveThinking is true; uses adaptive thinking with native xhigh effort for Claude Fable 5; allows built-in adaptive models to opt out with compat.forceAdaptiveThinking false; preserves thinking.type=disabled when reasoning is off regardless of override | ADAPTED (`testAnthropicForceAdaptiveThinking`) |
| `test/anthropic-long-cache-retention-e2e.test.ts` | 1 | covers every generated anthropic-messages model | ADAPTED (`testAnthropicLongCacheRetentionModelCoverage`; live credential probes remain external) |
| `test/anthropic-oauth.test.ts` | 3 | keeps the localhost redirect_uri for manual callback login; omits scope from refresh token requests; anthropicOAuth.login resolves through the manual_code prompt and aborts it after settling | PENDING |
| `test/anthropic-opus-4-8-smoke.test.ts` | 1 | streams Claude Opus 4.8 with reasoning enabled | PENDING |
| `test/anthropic-sse-parsing.test.ts` | 3 | repairs malformed SSE JSON and malformed streamed tool JSON; preserves refusal stop details from message_delta; ignores unknown SSE events after message_stop | PENDING |
| `test/anthropic-temperature-compat.test.ts` | 6 | omits temperature for Claude Opus 4.7; omits temperature for Claude Opus 4.8; omits default temperature for Claude Opus 4.7; keeps temperature for Claude Opus 4.6; keeps temperature for Claude Sonnet 4.6 | ADAPTED (`testAnthropicTemperatureCompat`) |
| `test/anthropic-thinking-disable.test.ts` | 7 | sends thinking.type=disabled for budget-based reasoning models when thinking is off; sends thinking.type=disabled for adaptive reasoning models when thinking is off; sends thinking.type=disabled for Claude Opus 4.8 when thinking is off; omits thinking.type=disabled for Claude Fable 5 when thinking is off; uses adaptive thinking for Claude Opus 4.8 when reasoning is enabled | ADAPTED (`testAnthropicThinkingDisablePayload`) |
| `test/anthropic-tool-name-normalization.test.ts` | 4 | should normalize user-defined tool matching CC name (todowrite -> TodoWrite -> todowrite); should handle pi built-in tools; should NOT map find to Glob; should handle custom tools | PARTIAL (`testAnthropicOAuthToolNameNormalization`; inbound live OAuth round-trip remains credential-gated) |
| `test/azure-openai-base-url.test.ts` | 11 | normalizes Cognitive Services root endpoints to /openai/v1; normalizes Azure OpenAI root endpoints to /openai/v1; normalizes /openai to /openai/v1; preserves /openai/v1 endpoints; preserves explicit non-Azure proxy paths | PENDING |
| `test/bedrock-convert-messages.test.ts` | 9 | skips unknown user content blocks instead of throwing; skips unknown assistant content blocks instead of throwing; replaces user messages with only unknown content blocks with a placeholder; replaces blank user string content with a placeholder; filters blank user text blocks when other content remains | PARTIAL (pluggable transport) |
| `test/bedrock-custom-headers.test.ts` | 6 | VC1: registers a build-step middleware that injects the caller header (happy path); VC2: skips reserved headers case-insensitively while applying allowed ones; VC3: registers no middleware when headers is undefined; VC3: registers no middleware when headers is empty; VC3 (structural guard): passes through unchanged when the request has no headers | PARTIAL (pluggable transport) |
| `test/bedrock-endpoint-resolution.test.ts` | 7 | assigns eu-central-1 runtime URLs to built-in EU inference profiles; does not pin standard AWS endpoints when AWS_REGION is configured; derives region from a built-in EU endpoint when no region or profile is configured; handles missing regions for explicit, scoped, and ambient profiles; still passes custom Bedrock endpoints through to the SDK client | PARTIAL (pluggable transport) |
| `test/bedrock-models.test.ts` | 1 | should get all available Bedrock models | PARTIAL (pluggable transport) |
| `test/bedrock-thinking-payload.test.ts` | 10 | uses adaptive thinking for Claude Opus 4.8 when reasoning is enabled; maps xhigh reasoning to effort=xhigh for Claude Opus 4.8; uses adaptive thinking for Claude Fable 5 when reasoning is enabled; maps xhigh reasoning to effort=xhigh for Claude Fable 5; omits display for GovCloud model ids on non-adaptive Claude thinking | PARTIAL (pluggable transport) |
| `test/cache-retention.test.ts` | 19 | cache retention payload behavior across Anthropic/OpenAI/Responses | PARTIAL (`testAnthropicCacheRetentionNoneAndLongCompat`, `testEnvDrivenPromptCacheRetention`; live/API-specific cases pending) |
| `test/compat-env.test.ts` | 1 | dispatches unknown providers through the legacy API registry | PENDING |
| `test/context-overflow.test.ts` | 29 | claude-haiku-4-5 - should detect overflow via isContextOverflow; claude-sonnet-4 - should detect overflow via isContextOverflow; gpt-4o-mini - should detect overflow via isContextOverflow; gpt-4o - should detect overflow via isContextOverflow; gpt-4o-mini - should detect overflow via isContextOverflow | PENDING |
| `test/cross-provider-handoff.test.ts` | 0 |  | PENDING |
| `test/empty.test.ts` | 88 | should handle empty content array; should handle empty string content; should handle whitespace-only content; should handle empty assistant message in conversation; should handle empty content array | PENDING |
| `test/env-api-keys.test.ts` | 3 | does not treat generic GitHub tokens as GitHub Copilot credentials; resolves GitHub Copilot credentials from COPILOT_GITHUB_TOKEN; resolves ZAI China Coding Plan credentials from ZAI_CODING_CN_API_KEY | PENDING |
| `test/faux-provider.test.ts` | 22 | registers a custom provider and estimates usage; supports helper blocks for text, thinking, and tool calls; supports multiple models with per-model reasoning and model-aware factories; rewrites api, provider, and model on returned messages; consumes queued responses in order and errors when exhausted | PENDING |
| `test/fireworks-models.test.ts` | 9 | Fireworks Kimi/router metadata, env key, session-affinity and tool compat | PARTIAL (`testFireworksKimiK26ModelMetadataAndCompat`, `testFireworksAnthropicToolCompatRequestShape`; local HTTP header capture pending) |
| `test/github-copilot-anthropic.test.ts` | 3 | applies Copilot-specific adaptive thinking effort overrides; uses Bearer auth, Copilot headers, and valid Anthropic Messages payload; omits interleaved-thinking beta for adaptive-thinking models | PENDING |
| `test/github-copilot-oauth.test.ts` | 6 | filters models to the authenticated account picker catalog; reports device-code details through onDeviceCode; rejects a non-http(s) verification_uri before it reaches onDeviceCode; normalizes verification_uri before it reaches onDeviceCode; polls immediately and increases the interval after slow_down | PENDING |
| `test/google-shared-convert-tools.test.ts` | 7 | strips JSON Schema meta keys from parameters when useParameters=true; recursively strips nested JSON Schema meta keys; preserves $ref while stripping meta keys; does not mutate the original Tool.parameters object; preserves $schema in parametersJsonSchema when useParameters=false | PENDING |
| `test/google-shared-gemini3-unsigned-tool-call.test.ts` | 4 | unsigned Gemini 3/Vertex tool calls do not add validator bypass; valid signatures preserved; non-Gemini-3 unsigned calls remain unsigned | ADAPTED (`testGoogleGemini3UnsignedToolCalls`) |
| `test/google-shared-image-tool-result-routing.test.ts` | 2 | keeps separate synthetic image turn for Gemini 2.x Google API models; nests image tool results for Gemini 3 Google API models | PENDING |
| `test/google-thinking-disable.test.ts` | 7 | disables thinking for budget-based reasoning models; disables thinking for adaptive reasoning models; disables thinking for Gemini 2.5; disables thinking for Gemini 3.x; does not error when thinking is off for Gemini 3.1 Pro | PENDING |
| `test/google-thinking-signature.test.ts` | 5 | treats part.thought === true as thinking; does not treat thoughtSignature alone as thinking; does not treat empty/missing signatures as thinking if thought is not set; preserves the existing signature when subsequent deltas omit thoughtSignature; updates the signature when a new non-empty signature arrives | PENDING |
| `test/google-vertex-api-key-resolution.test.ts` | 8 | falls back to ADC when options.apiKey is a placeholder marker; falls back to ADC when options.apiKey is the gcp-vertex-credentials marker; falls back to ADC when GOOGLE_CLOUD_API_KEY is a placeholder marker; still uses the API key client for real API keys; does not forward generated Vertex base URL placeholders | PENDING |
| `test/image-tool-result.test.ts` | 26 | should handle tool result with only image; should handle tool result with text and image; should handle tool result with only image; should handle tool result with text and image; should handle tool result with only image | PENDING |
| `test/images-models.test.ts` | 6 | image registry/provider/auth model behavior | PARTIAL (`ImageRegistryTests`; full createImagesModels auth manager not ported) |
| `test/images.test.ts` | 3 | should generate a basic image; should handle text plus image output; should handle image input | PENDING |
| `test/interleaved-thinking.test.ts` | 4 | should do interleaved thinking on Claude Opus 4.5; should do interleaved thinking on Claude Opus 4.6; should do interleaved thinking on Claude Opus 4.5; should do interleaved thinking on Claude Opus 4.6 | PENDING |
| `test/lazy-module-load.test.ts` | 5 | does not load provider SDKs when importing the root barrel; does not load provider SDKs when building all builtin providers; does not load provider SDKs when importing the compat entrypoint; loads only the Anthropic SDK when streaming through the lazy API wrapper; loads only the Anthropic SDK when dispatching through streamSimple | PENDING |
| `test/mistral-reasoning-mode.test.ts` | 7 | uses reasoning_effort for Mistral Small 4; omits reasoning controls for Mistral Small 4 when thinking is off; uses prompt_mode for Magistral reasoning models; uses reasoning_effort for Mistral Medium 3.5; omits reasoning controls for Mistral Medium 3.5 when thinking is off | PENDING |
| `test/mistral-tool-schema.test.ts` | 1 | strips TypeBox symbol keys before the SDK validates tool schemas | PENDING |
| `test/models-runtime.test.ts` | 16 | registers, replaces, and deletes providers; lists and finds models per provider; swallows provider source failures for both all-provider and single-provider listing; refresh() updates dynamic providers; single-provider refresh failures reject; resolves auth: stored credential owns the provider, ambient only when nothing stored | PENDING |
| `test/node-http-proxy.test.ts` | 4 | respects NO_PROXY exclusions; resolves HTTP and HTTPS proxy URLs; prefers scoped proxy env aliases before process env aliases; rejects SOCKS and PAC proxy URLs explicitly | PENDING |
| `test/oauth-auth.test.ts` | 8 | anthropic toAuth derives the api key from the access token; openai-codex toAuth derives the api key from the access token; github-copilot toAuth derives baseUrl from the token proxy endpoint; github-copilot toAuth falls back to the enterprise domain, then the individual endpoint; anthropic refresh exchanges the refresh token and returns a typed credential | PENDING |
| `test/oauth-device-code.test.ts` | 2 | polls immediately and returns the completed value; cancels an in-flight wait | PENDING |
| `test/openai-codex-cache-affinity-e2e.test.ts` | 0 |  | PARTIAL (pluggable transport) |
| `test/openai-codex-oauth.test.ts` | 8 | logs in with the OpenAI Codex device code flow; offers browser login first and uses the selected OpenAI Codex device code flow; cancels when OpenAI Codex login method selection is cancelled; cancels the OpenAI Codex device code flow while waiting; times out the OpenAI Codex device code flow after 15 minutes | PENDING |
| `test/openai-codex-stream.test.ts` | 16 | streams SSE responses into AssistantMessageEventStream; completes after response.completed even when the SSE body stays open; maps response.incomplete to stopReason length even when the SSE body stays open; aborts SSE fetch when response headers do not arrive; aborts SSE body reads after response headers arrive | PARTIAL (pluggable transport) |
| `test/openai-completions-cache-control-format.test.ts` | 3 | applies Anthropic-style cache markers when model compat enables them; preserves Anthropic-style cache markers for OpenRouter Anthropic models; omits Anthropic-style cache markers when cacheRetention is none | ADAPTED (`testOpenAICompletionsAnthropicCacheControlFormat`) |
| `test/openai-completions-empty-tools.test.ts` | 9 | omits tools field when context.tools is an empty array; omits tools field when context.tools is undefined; does not send default max token fields; sends explicit maxTokens; uses conservative OpenAI-compatible fields for Cloudflare AI Gateway /compat models | PARTIAL (`testOpenAICompletionsEmptyToolsAndMaxTokens`, `testOpenAICompat0802Params`; client-options/header assertions pending) |
| `test/openai-completions-prompt-cache.test.ts` | 9 | prompt_cache_key/retention direct OpenAI, proxy omission, env long retention, session affinity headers | PARTIAL (`testOpenAICompletionsPromptCacheParity`, `testEnvDrivenPromptCacheRetention`; private client header assertions partially covered by request construction) |
| `test/openai-completions-reasoning-details.test.ts` | 1 | preserves reasoning_details that arrive before their matching tool call | ADAPTED (`testOpenAIReasoningDetailsStreamingAndReplay`) |
| `test/openai-completions-response-model.test.ts` | 3 | routed chunk.model on responseModel; echo requested id leaves responseModel undefined; empty/missing model ignored | ADAPTED (`testOpenAISSEProcessing`, `testOpenAIResponseModelEchoAndEmptyIgnored`) |
| `test/openai-completions-retry.test.ts` | 2 | disables SDK retries by default; honors explicit provider retry settings | PENDING |
| `test/openai-completions-thinking-as-text.test.ts` | 3 | serializes same-model thinking-plus-text replay as assistant text parts; serializes same-model thinking-only replay as assistant text parts; reaches the endpoint when replay contains both thinking and text | ADAPTED (`testOpenAIThinkingAsTextReplay`; endpoint smoke covered by provider stream tests) |
| `test/openai-completions-tool-choice.test.ts` | 41 | forwards toolChoice from simple options to payload; omits strict when compat disables strict mode; maps groq qwen3 reasoning levels to default reasoning_effort; keeps normal reasoning_effort for groq models without compat mapping; enables tool_stream for supported z.ai models with tools | ADAPTED |
| `test/openai-completions-tool-result-images.test.ts` | 1 | batches tool-result images after consecutive tool results | PENDING |
| `test/openai-responses-cache-affinity-e2e.test.ts` | 1 | handles direct OpenAI Responses requests with aligned cache-affinity identifiers | PENDING |
| `test/openai-responses-copilot-provider.test.ts` | 7 | omits reasoning when no reasoning is requested; sets cache-affinity headers for official OpenAI Responses requests with a sessionId; clamps prompt_cache_key to OpenAI; sets cache-affinity headers for proxy OpenAI Responses requests with a sessionId; can omit the session_id header while preserving other cache-affinity headers | PENDING |
| `test/openai-responses-foreign-toolcall-id.test.ts` | 1 | hashes foreign Copilot tool item IDs into bounded Codex-safe fc_<hash> shape | ADAPTED (`testOpenAIResponsesForeignToolCallIDNormalization`) |
| `test/openai-responses-message-id.test.ts` | 1 | generates unique fallback message IDs for multiple text blocks in one assistant turn | ADAPTED (`testOpenAIResponsesFallbackMessageIDs`) |
| `test/openai-responses-partial-json-cleanup.test.ts` | 1 | removes partialJson from persisted tool-call blocks at output_item.done | ADAPTED (`testOpenAIResponsesPartialJSONCleanup`) |
| `test/openai-responses-reasoning-replay-e2e.test.ts` | 3 | skips reasoning-only history after an aborted turn; handles same-provider different-model handoff with tool calls; handles cross-provider handoff from Anthropic to OpenAI Codex | PENDING |
| `test/openai-responses-terminal-event.test.ts` | 5 | rejects streams before terminal event; wrapper early EOF error; completed/incomplete/failed terminal handling | ADAPTED (`testOpenAIResponsesTerminalEvents`) |
| `test/openai-responses-tool-result-images.test.ts` | 4 | tool result images stay in function_call_output for Responses providers | PARTIAL (`testOpenAIResponsesToolResultImagesStayInFunctionCallOutput`; live provider response assertions are LIVE-GATED/pending) |
| `test/openrouter-cache-write-repro.test.ts` | 1 | regression: preserves cache_write_tokens on openai-completions stream path | PENDING |
| `test/openrouter-images.test.ts` | 3 | returns text plus images in final output; passes through abort signal and returns aborted result; generateImages resolves the final assistant images result | PENDING |
| `test/overflow.test.ts` | 13 | detects explicit Ollama prompt-too-long errors; detects Together AI context length errors; detects LiteLLM-wrapped OpenAI maximum context length errors; detects OpenAI-compatible parenthesized maximum context length errors; detects OpenRouter Poolside maximum allowed input length errors | ADAPTED (`OverflowTests`) |
| `test/providers.test.ts` | 9 | builtin provider/model registry and auth helper behavior | PARTIAL (`testEnvApiKeyAuthHelper`, registry/env tests; full Models runtime API pending) |
| `test/responseid.test.ts` | 11 | live responseId E2E across providers | LIVE-GATED (`testMistralRequestAndSSEProcessing` covers deterministic Mistral id/model parsing; live API-key cases pending) |
| `test/stream.test.ts` | 164 | should complete basic text generation; should handle tool calling; should handle streaming; should handle thinking; should handle multi-turn with thinking and tools | PENDING |
| `test/supports-xhigh.test.ts` | 16 | includes xhigh for Anthropic Opus 4.6 on anthropic-messages API; includes xhigh for Anthropic Opus 4.8 on anthropic-messages API; includes xhigh for Anthropic Opus 4.8 on anthropic-messages API; includes xhigh but not off for Anthropic Claude Fable 5 on anthropic-messages API; does not include xhigh for Claude Sonnet 4.5 | ADAPTED (`testUpstreamSupportedThinkingLevels`) |
| `test/together-models.test.ts` | 3 | default Kimi K2.6 metadata; Together reasoning controls; TOGETHER_API_KEY env resolution | ADAPTED (`ProviderMetadataTests`) |
| `test/tokens.test.ts` | 18 | should include token stats when aborted mid-stream; should include token stats when aborted mid-stream; should include token stats when aborted mid-stream; should include token stats when aborted mid-stream; should include token stats when aborted mid-stream | PENDING |
| `test/tool-call-id-normalization.test.ts` | 4 | github-copilot -> openrouter should normalize pipe-separated IDs; github-copilot -> openai-codex should normalize pipe-separated IDs; openrouter should handle prefilled context with long pipe-separated IDs; openai-codex should handle prefilled context with long pipe-separated IDs | PARTIAL (`testOpenAIToolCallIDNormalizationFromResponses`; live handoff cases are LIVE-GATED/pending) |
| `test/tool-call-without-result.test.ts` | 22 | should filter out tool calls without corresponding tool results; should filter out tool calls without corresponding tool results; should filter out tool calls without corresponding tool results; should filter out tool calls without corresponding tool results; should filter out tool calls without corresponding tool results | PENDING |
| `test/total-tokens.test.ts` | 27 | claude-sonnet-4-5 - should return totalTokens equal to sum of components; gpt-4o-mini - should return totalTokens equal to sum of components; claude-haiku-4.5 - should return totalTokens equal to sum of components; gpt-4o-mini - should return totalTokens equal to sum of components; gemini-2.0-flash - should return totalTokens equal to sum of components | PENDING |
| `test/transform-messages-copilot-openai-to-anthropic.test.ts` | 4 | converts thinking blocks to plain text when source model differs; removes thoughtSignature from tool calls when migrating between models; adds synthetic tool results for trailing orphaned tool calls; adds synthetic results only for trailing tool calls that are still missing results | PENDING |
| `test/unicode-surrogate.test.ts` | 66 | should handle emoji in tool results; should handle real-world LinkedIn comment data with emoji; should handle unpaired high surrogate (0xD83D) in tool results; should handle emoji in tool results; should handle real-world LinkedIn comment data with emoji | PENDING |
| `test/validation.test.ts` | 3 | still validates when Function constructor is unavailable; coerces serialized plain JSON schemas with AJV-compatible primitive rules; rejects invalid coercions for serialized plain JSON schemas | ADAPTED (`testToolValidationCoercionParity`; Function-constructor fallback N/A in Swift) |
| `test/xhigh.test.ts` | 3 | should work with openai-responses; should error with openai-responses when using xhigh; should error with openai-completions when using xhigh | PENDING |
| `test/xiaomi-models.test.ts` | 2 | keeps mimo-v2-flash on the API billing provider; omits mimo-v2-flash from token plan providers | ADAPTED (`testXiaomiMiMoModelPlacement`) |
| `test/xiaomi-token-plan-ams-anthropic-empty-signature-smoke.test.ts` | 1 | reproduces empty thinking signatures and preserves them for replay | LIVE-GATED (`testXiaomiTokenPlanAMSAnthropicEmptySignatureSmokeLive`; deterministic replay covered by `testAnthropicEmptyThinkingSignatureCompat`) |
| `test/zen.test.ts` | 0 |  | PENDING |
