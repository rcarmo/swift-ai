name: CI

on:
  push:
  pull_request:
  workflow_dispatch:
  schedule:
    # Weekly dependency/SBOM/OSV maintenance run; independent of developer final-only CI policy.
    - cron: '17 4 * * 1'

jobs:
  static-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.x'
      - run: make static-check
      - run: make sbom-check
      - uses: actions/upload-artifact@v5
        with:
          name: swift-ai-sbom-${{ github.sha }}
          path: |
            .artifacts/sbom/swift-ai.cdx.json
            .artifacts/sbom/swift-ai.cdx.json.sha256
            .artifacts/sbom/swift-ai-security-scan.json
            .artifacts/sbom/swift-ai-license-review.json
            .artifacts/sbom/osv-scanner.json
          if-no-files-found: error
          retention-days: 30

  swift-test:
    strategy:
      matrix:
        # macOS hosted CI is disabled by project policy; routine CI keeps Ubuntu + static checks only.
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - run: swift --version
      - run: make check
