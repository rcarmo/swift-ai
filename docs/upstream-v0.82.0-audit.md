# Upstream pi-ai v0.82.0 release parity audit

Baseline: accepted official release `v0.81.1` / `20be4b18d4c57487f8993d2762bace129f0cf7c6`.
Target: official release `v0.82.0` / `083e61621276bff9f6faefab87ce07fcd98734e2`.
Scope: release-only audit pinned to `083e6162`; no commits beyond the tag were considered.

The final `packages/ai` delta is exactly 96 changed paths.

## Exact changed-path disposition matrix

| Upstream paths | Material delta | Swift disposition |
| --- | --- | --- |
| `CHANGELOG.md`, `README.md`, `package.json`, `tsconfig.build.json` | Release metadata, package exports, and documented v0.82.0 API surface. | Reflected in `STATUS.json`, `PARITY.md`, and this audit. No SwiftPM packaging/runtime change required. |
| `scripts/check-model-data.ts`, `scripts/model-data.ts`, `scripts/generate-models.ts`, `src/providers/data-json.d.ts`, `src/models.generated.ts`, all changed `src/providers/*.models.ts` | Generated model data validation/type shape changes and catalog refreshes. | Regenerated exact-tag text catalog from v0.82.0 generator output: `scripts/models.v0.82.0.json`, `scripts/upstream-models.083e616.json`, `Sources/SwiftAI/ModelsGenerated.swift`. `scripts/audit-parity.py` compares Swift source+embedded registry against exact upstream source: **1116/1116 text provider/id pairs**, **37 providers**, **9 APIs**. Metadata representatives include Qwen Token Plan, OpenCode Go Responses, Google computer-use, OpenRouter Ling, Kimi/Moonshot/OpenRouter/Vercel entries. |
| `scripts/generate-image-models.ts`, `src/image-models.generated.ts`, changed image model data/tests | Image catalog refresh and generated image JSON typing. | Regenerated exact-tag image catalog: `scripts/image-models.v0.82.0.json`, `scripts/upstream-image-models.083e616.json`, `Sources/SwiftAI/ImageModelsGenerated.swift`. Comparator checks **40/40 image provider/id pairs** and representatives `krea/krea-2-large`, `openrouter/auto-beta`, `microsoft/mai-image-2.5-pro`. |
| `src/api/constrained-sampling.ts`, constrained sampling changes in `src/api/openai-completions.ts`, `src/api/openai-responses-shared.ts`, `src/api/openai-responses.ts`, `src/api/azure-openai-responses.ts`, `src/api/openai-codex-responses.ts`, `src/api/anthropic-messages.ts`, `src/api/mistral-conversations.ts`, `src/api/bedrock-converse-stream.ts`, `test/constrained-sampling.test.ts`, related tool tests | New provider-side constrained sampling for JSON-schema strict tools and OpenAI grammar tools. | Added Swift `ConstrainedSamplingConfig`, `GrammarVariants`, and `Tool.constrainedSampling`. OpenAI Completions now emits strict JSON-schema tools and OpenAI custom grammar tools when supported, and production validation rejects required strict tools on unsupported providers. Tests cover grammar input inference, grammar tool request shape, and strict JSON-schema request shape. N/A: full SDK-specific Anthropic/Bedrock/Codex client integrations remain represented through existing pluggable request builders/transports; Swift added the portable public model and OpenAI-compatible path. |
| `src/auth/oauth/openrouter.ts`, OAuth registry/all-provider wiring, `test/openrouter-oauth.test.ts` | New OpenRouter PKCE OAuth that exchanges authorization code for a permanent API key. | Added `OpenRouterOAuthProvider`, registered in `SwiftAI.bootstrap()`. SwiftPM portability uses host prompt/callback-code exchange instead of embedding a local HTTP callback server. Tests cover key-exchange request/response and registry wiring. |
| `src/auth/oauth/kimi-coding.ts`, Kimi provider auth wiring, `test/kimi-coding-oauth.test.ts` | New Kimi Code subscription OAuth device-code flow, token refresh with retry, and bearer header auth. | Added `KimiCodingOAuthProvider`, registered in `SwiftAI.bootstrap()`. It supports device authorization, complete verification URI, token polling transitions, refresh retry on transient failures, and bearer access token. Tests cover device authorization, token polling, refresh retry, and trusted URL handling. |
| `src/utils/provider-retry.ts`, provider request changes in `src/api/openai-completions.ts`, `src/api/openai-responses.ts`, `src/api/azure-openai-responses.ts`, `src/api/anthropic-messages.ts`, `src/api/openrouter-images.ts`, `test/provider-retry.test.ts`/retry deltas | Abortable SDK/provider retry wrapper, provider-requested retry delay caps, retry headers, 408/409/429/5xx, and DNS/transport retry wording. | Added `ProviderRetry` and `ProviderRetryError` for abortable provider retry semantics, retry headers (`retry-after-ms`, `retry-after`), `x-should-retry`, 408/409/429/5xx, retry delay cap errors, plus DNS retry classifier phrases (`getaddrinfo`, `ENOTFOUND`, `EAI_AGAIN`). Tests cover retry classification, delay parsing/caps, and successful retry. Existing `HTTPRetry` remains the URLSession transport path. |
| `src/utils/retry.ts`, `test/retry.test.ts` | Retry classifier expanded for DNS failures. | Added DNS phrases to Swift `AssistantErrorRetryClassifier` and tests. |
| `src/api/openai-completions.ts`, `test/openai-completions-*`, `test/tool-call-without-result.test.ts`, stream/error/usage fixtures | Provider/model/tool/stream/error/usage fixes around request shape, constrained tools, DNS retry, and tool-call behavior. | Existing Swift request builders already cover tool history, stream usage, errors, surrogate sanitization, and missing tool-result synthesis. New constrained sampling and retry-specific regressions were added. |
| `src/api/pi-messages.ts`, `src/api/openai-codex-responses.ts`, `src/auth/helpers.ts`, `src/models-store.ts`, `src/providers/amazon-bedrock.ts`, `src/providers/openrouter-images.ts`, `src/providers/radius-config.ts`, remaining tests (`abort`, `empty`, `stream`, `tokens`, `total-tokens`, `unicode-surrogate`, `image-tool-result`, provider metadata) | Runtime refinements and test fixture updates outside the new portable public surfaces. | Reviewed for Swift applicability. Existing Swift implementations already cover portable behavior: pluggable Codex/Bedrock transports, PiMessages payload/error handling, Radius config, message usage, unicode sanitization, stream parsers, and image tool result support. N/A decisions are documented here rather than adding Node/Bun-specific callback server or SDK retry internals. |

## Validation requirements

- `scripts/audit-parity.py` enforces exact text and image catalog parity against `scripts/upstream-models.083e616.json` and `scripts/upstream-image-models.083e616.json`.
- Local gates must pass: `swift build -Xswiftc -warnings-as-errors`, `swift test`, deterministic `swift test` repeats, `make check`, `scripts/audit-parity.py`, `scripts/static-check.py`, and zero hidden `XCTSkip` matches.
- Acceptance requires GitHub Actions `static-check`, Ubuntu Swift tests, and macOS Swift tests all green for the pushed commit.
